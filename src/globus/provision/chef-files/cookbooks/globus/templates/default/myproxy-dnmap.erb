#!/bin/sh

username=$1
if [ X"$username" = X ]; then
    # no username given
    exit 1
fi

# DN must match the signing policy for your CA.
echo "<%= @subject.select{|k,v| k != "cn"}.collect{|x| "/"+x[0].upcase+"="+x[1]}.join "" %>/CN=${username}"

exit 0
